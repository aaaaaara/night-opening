import * as Styles from './Header.styles';

function Header() {
  return (
    <Styles.Container>
      <Styles.Inner>Night Opening</Styles.Inner>
    </Styles.Container>
  );
}

export default Header;
